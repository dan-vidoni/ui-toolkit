const style = (theme) => ({
    label: {
        color: theme.palette.text.primary,
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '2px',
        lineHeight: '16px',
        textTransform: 'uppercase',
        fontFamily: "'Open Sans'"
    },
});

export default style
